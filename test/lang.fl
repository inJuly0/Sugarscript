// single line comment 
/* multi
line 
comment */

let a = 2
let b = "string 
    with 
    multiple 
    lines"

let c:bool = true 

let myArray: [num] = [1, 2, 3, 4, 5]

const myString: string = 'hello'

foreach(a: myArray):
    print(a)


for (i = 1, 10): 
    print(i)


const myObject = {
    a: 1, 
    b: 2,
    c: 4
}

print(myObject.a)

while(myBool)
    //do this


if cond:
    //do this 
elif cond2:
    //do that 
else:
    //come here and do this thing


fun myFunc(a: num, b: num) -> num:
    return a + b

fun optArg(a: num, b?:num):
    return a * (b or 1)


class Monster:
    readonly sound: string
    
    init(sound: string):
        @sound = sound
    
    makeSound():
        print(@sound)
    


class Goblin <- Monster:
    init()
        super('hihi')