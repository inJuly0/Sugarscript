// single line comment

/* multi line
comment */

// assignment 

myNumber = 12
global globalNumber = 999
myNumber = 12
myString = 'hello'

fun myAdder(a, b) 
    return a + b

fun emptyFunc()
    pass

// lambdas

myAdder = (a, b:num) -> a + b

myObject = {
    name'helo',
    occupation'jelo',
    ['123']456,
    nestedObj{
        name'Melo'
        get name()
            return name
    }
}    

enum Color
    RED, BLUE,
    BLACK, GREEN,
    YELLOW, ORANGE


# loops 

for i = 1, 10
    print(i)

myArray = [1, 2, 4]

for e in myArray 
    print(e)

n = 0

while n < 10 
    print(n)
    n += 1


# if/else/elseif

if a == 1
    //do this 
elif a == 2
    // do this instead
else 
    //do this 

# switch case

match myVar
    'possible value'
        fall 
    'another possible value':
        //do this 
    default
        print('in default')

# classes and inheritance

class Animal
    sound
    static animalCount = 0
    
    init(sound)
        @sound = sound 
        animalCount += 1

    makeSound()
        print(@sound)


class Dog <- Animal
    bark()
        makeSound('woof')

    static printDogStuff()
        print('dogs are nice and kool')

fun love.load()
    Resources.load()

fun love.draw()
    love.graphics.print('hello world')

fun love.update(dt)
    timeElapsed += dt
